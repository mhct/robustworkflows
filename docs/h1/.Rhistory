previous_break <- 0
real_composition_times <- data.frame()
index <- 0
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
if (length(selected_measurements)) == 0 ) {
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
if (length(selected_measurements) == 0 ) {
selected_measurements <- 0
}
real_tempdf <- data.frame(id=index, composition_time=selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
View(real_composition_times)
h1 <- experiment_results_raw[experiment_results_raw$X == "h1_250_1",]
h1_ordered <- relativeTime(h1[order(h1[,c("start_time_millis")]),])
seconds_breaks <- seq(from=1000,to=max_time, by=1000)
previous_break <- 0
real_composition_times <- data.frame()
index <- 0
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
if (length(selected_measurements) == 0 ) {
selected_measurements <- 0
}
real_tempdf <- data.frame(id=index, composition_time=selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
index <- index + 1
}
View(real_composition_times)
ggplot(real_composition_times) + geom_boxplot(aes(x=factor(id), y=composition_time))
h1 <- experiment_results_raw[experiment_results_raw$X == "h1_250_1",]
h1_ordered <- relativeTime(h1[order(h1[,c("start_time_millis")]),])
seconds_breaks <- seq(from=1000,to=max_time, by=100)
previous_break <- 0
real_composition_times <- data.frame()
index <- 0
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
if (length(selected_measurements) == 0 ) {
selected_measurements <- 0
}
real_tempdf <- data.frame(id=index, composition_time=selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
index <- index + 1
}
ggplot(real_composition_times) + geom_boxplot(aes(x=factor(id), y=composition_time))
h1 <- experiment_results_raw[experiment_results_raw$X == "h1_250_1",]
h1_ordered <- relativeTime(h1[order(h1[,c("start_time_millis")]),])
seconds_breaks <- seq(from=1000,to=max_time, by=10000)
previous_break <- 0
real_composition_times <- data.frame()
index <- 0
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
if (length(selected_measurements) == 0 ) {
selected_measurements <- 0
}
real_tempdf <- data.frame(id=index, composition_time=selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
index <- index + 1
}
ggplot(real_composition_times) + geom_boxplot(aes(x=factor(id), y=composition_time))
h1 <- experiment_results_raw[experiment_results_raw$X == "h1_250_1",]
h1_ordered <- relativeTime(h1[order(h1[,c("start_time_millis")]),])
seconds_breaks <- seq(from=1000,to=max_time, by=10000)
previous_break <- 0
real_composition_times <- data.frame()
index <- 0
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (length(selected_measurements) == 0 ) {
selected_measurements <- 0
}
real_tempdf <- data.frame(id=index, composition_time=selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
index <- index + 1
}
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (length(selected_measurements) == 0 ) {
selected_measurements <- 0
}
real_tempdf <- data.frame(id=index, composition_time=selected_measurements$REAL_TIME_TO_SERVE_COMPOSITION, composition_size=selected_measurements$composition_size)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
index <- index + 1
}
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (length(selected_measurements) == 0 ) {
selected_measurements$REAL_TIME_TO_SERVE_COMPOSITION <- 0
selected_measurements$composition_size <- 0
}
real_tempdf <- data.frame(id=index, composition_time=selected_measurements$REAL_TIME_TO_SERVE_COMPOSITION, composition_size=selected_measurements$composition_size)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
index <- index + 1
}
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
selected_measurements
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis <= 1000, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
selected_measurements
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (length(selected_measurements) > 0 ) {
real_tempdf <- data.frame(id=index,
composition_time=selected_measurements$REAL_TIME_TO_SERVE_COMPOSITION,
composition_size=selected_measurements$composition_size)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
index <- index + 1
}
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (length(selected_measurements) > 0 ) {
real_tempdf <- data.frame(id=index,  selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
index <- index + 1
}
selected_measurements
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis <= 1000, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
selected_measurements
a<-data.frame()
rbind(a, selected_measurements)
rbind(a, x=1, selected_measurements)
rbind(a, x=rep(1, times=length(selected_measurements)), selected_measurements)
data.frame(x=rep(1, times=(length(selected_measurements)), selected_measurements)
)
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis <= 1000, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
View(selected_measurements)
cbind(x=1, selected_measurements)
seconds_breaks <- seq(from=1000,to=max_time, by=10000)
previous_break <- 0
real_composition_times <- data.frame()
index <- 0
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (length(selected_measurements) > 0 ) {
real_tempdf <- cbind(id=index,  selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
index <- index + 1
}
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (length(selected_measurements) > 0 ) {
real_tempdf <- cbind(id=1,  selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
index <- index + 1
}
h1_ordered <- relativeTime(h1[order(h1[,c("start_time_millis")]),])
seconds_breaks <- seq(from=1000,to=max_time, by=10000)
previous_break <- 0
real_composition_times <- data.frame()
index <- 0
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (length(selected_measurements) > 0 ) {
real_tempdf <- cbind(id=1,  selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
index <- index + 1
}
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (length(selected_measurements) > 0 ) {
print("processing")
real_tempdf <- cbind(id=1,  selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
index <- index + 1
}
selected_measurements
length()elected_measurements
length(elected_measurements)
length(selected_measurements)
nrow(selected_measurements)
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (nrow(selected_measurements) > 0 ) {
print("processing")
real_tempdf <- cbind(id=1,  selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
index <- index + 1
}
View(real_composition_times)
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (nrow(selected_measurements) > 0 ) {
print("processing")
real_tempdf <- cbind(id=index,  selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
index <- index + 1
}
View(real_composition_times)
seconds_breaks <- seq(from=1000,to=max_time, by=1000)
previous_break <- 0
real_composition_times <- data.frame()
index <- 0
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (nrow(selected_measurements) > 0 ) {
print("processing")
real_tempdf <- cbind(id=index,  selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
index <- index + 1
}
View(real_composition_times)
ggplot(real_composition_times) + geom_boxplot(aes(x=factor(id), y=composition_time), color=composition_size)
ggplot(real_composition_times) + geom_boxplot(aes(x=factor(id), y=composition_time, color=composition_size))
ggplot(real_composition_times) + geom_boxplot(aes(x=factor(id), y=REAL_TIME_TO_SERVE_COMPOSITION), color=composition_size)
ggplot(real_composition_times) + geom_boxplot(aes(x=factor(id), y=REAL_TIME_TO_SERVE_COMPOSITION, color=composition_size))
ggplot(real_composition_times) + geom_boxplot(aes(x=factor(id), y=REAL_TIME_TO_SERVE_COMPOSITION) + facet_grid(. ~ composition_size)
)
ggplot(real_composition_times) + geom_boxplot(aes(x=factor(id), y=REAL_TIME_TO_SERVE_COMPOSITION)) + facet_grid(. ~ composition_size)
seconds_breaks <- seq(from=1000,to=max_time, by=10000)
previous_break <- 0
real_composition_times <- data.frame()
index <- 0
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (nrow(selected_measurements) > 0 ) {
print("processing")
real_tempdf <- cbind(id=index,  selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
index <- index + 1
}
ggplot(real_composition_times) + geom_boxplot(aes(x=factor(id), y=REAL_TIME_TO_SERVE_COMPOSITION)) + facet_grid(. ~ composition_size)
View(`h1_ordered`)
seconds_breaks <- seq(from=1000,to=max_time, by=1000)
previous_break <- 0
real_composition_times <- data.frame()
index <- 0
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (nrow(selected_measurements) > 0 ) {
real_tempdf <- cbind(id=index,  selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
index <- index + 1
}
ggplot(real_composition_times) + geom_boxplot(aes(x=factor(id), y=REAL_TIME_TO_SERVE_COMPOSITION)) + facet_grid(. ~ composition_size)
h1 <- experiment_results_raw[experiment_results_raw$X == "h1_250_4",]
h1_ordered <- relativeTime(h1[order(h1[,c("start_time_millis")]),])
seconds_breaks <- seq(from=1000,to=max_time, by=1000)
previous_break <- 0
real_composition_times <- data.frame()
index <- 0
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (nrow(selected_measurements) > 0 ) {
real_tempdf <- cbind(id=index,  selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
index <- index + 1
}
ggplot(real_composition_times) + geom_boxplot(aes(x=factor(id), y=REAL_TIME_TO_SERVE_COMPOSITION)) + facet_grid(. ~ composition_size)
seconds_breaks <- seq(from=1000,to=max_time, by=10000)
previous_break <- 0
real_composition_times <- data.frame()
index <- 0
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (nrow(selected_measurements) > 0 ) {
real_tempdf <- cbind(id=index,  selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
index <- index + 1
}
ggplot(real_composition_times) + geom_boxplot(aes(x=factor(id), y=REAL_TIME_TO_SERVE_COMPOSITION)) + facet_grid(. ~ composition_size)
h1 <- experiment_results_raw[experiment_results_raw$X == "h1_1k_4",]
h1_ordered <- relativeTime(h1[order(h1[,c("start_time_millis")]),])
seconds_breaks <- seq(from=1000,to=max_time, by=10000)
previous_break <- 0
real_composition_times <- data.frame()
index <- 0
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (nrow(selected_measurements) > 0 ) {
real_tempdf <- cbind(id=index,  selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
index <- index + 1
}
ggplot(real_composition_times) + geom_boxplot(aes(x=factor(id), y=REAL_TIME_TO_SERVE_COMPOSITION)) + facet_grid(. ~ composition_size)
h1 <- experiment_results_raw[experiment_results_raw$X == "h1_4k_4",]
h1_ordered <- relativeTime(h1[order(h1[,c("start_time_millis")]),])
seconds_breaks <- seq(from=1000,to=max_time, by=10000)
previous_break <- 0
real_composition_times <- data.frame()
index <- 0
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (nrow(selected_measurements) > 0 ) {
real_tempdf <- cbind(id=index,  selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
index <- index + 1
}
ggplot(real_composition_times) + geom_boxplot(aes(x=factor(id), y=REAL_TIME_TO_SERVE_COMPOSITION)) + facet_grid(. ~ composition_size)
h1 <- experiment_results_raw[experiment_results_raw$X == "h1_16k_4",]
h1_ordered <- relativeTime(h1[order(h1[,c("start_time_millis")]),])
seconds_breaks <- seq(from=1000,to=max_time, by=10000)
previous_break <- 0
real_composition_times <- data.frame()
index <- 0
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (nrow(selected_measurements) > 0 ) {
real_tempdf <- cbind(id=index,  selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
index <- index + 1
}
ggplot(real_composition_times) + geom_boxplot(aes(x=factor(id), y=REAL_TIME_TO_SERVE_COMPOSITION)) + facet_grid(. ~ composition_size)
h1 <- experiment_results_raw[experiment_results_raw$X == "h1_64k_4",]
h1_ordered <- relativeTime(h1[order(h1[,c("start_time_millis")]),])
seconds_breaks <- seq(from=1000,to=max_time, by=10000)
previous_break <- 0
real_composition_times <- data.frame()
index <- 0
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (nrow(selected_measurements) > 0 ) {
real_tempdf <- cbind(id=index,  selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
index <- index + 1
}
ggplot(real_composition_times) + geom_boxplot(aes(x=factor(id), y=REAL_TIME_TO_SERVE_COMPOSITION)) + facet_grid(. ~ composition_size)
h1 <- experiment_results_raw[experiment_results_raw$X == "h1_64k_1",]
h1_ordered <- relativeTime(h1[order(h1[,c("start_time_millis")]),])
seconds_breaks <- seq(from=1000,to=max_time, by=10000)
previous_break <- 0
real_composition_times <- data.frame()
index <- 0
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (nrow(selected_measurements) > 0 ) {
real_tempdf <- cbind(id=index,  selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
index <- index + 1
}
ggplot(real_composition_times) + geom_boxplot(aes(x=factor(id), y=REAL_TIME_TO_SERVE_COMPOSITION)) + facet_grid(. ~ composition_size)
h1 <- experiment_results_raw[experiment_results_raw$X == "h1_64k_1",]
h1_ordered <- relativeTime(h1[order(h1[,c("start_time_millis")]),])
seconds_breaks <- seq(from=1000,to=max_time, by=1000)
previous_break <- 0
real_composition_times <- data.frame()
index <- 0
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (nrow(selected_measurements) > 0 ) {
real_tempdf <- cbind(id=index,  selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
index <- index + 1
}
ggplot(real_composition_times) + geom_boxplot(aes(x=factor(id), y=REAL_TIME_TO_SERVE_COMPOSITION)) + facet_grid(. ~ composition_size)
h1 <- experiment_results_raw[experiment_results_raw$X == "h1_1k_1",]
h1_ordered <- relativeTime(h1[order(h1[,c("start_time_millis")]),])
View(`h1_ordered`)
ggplot(h1_ordered) + geom_line(aes(x=log(start_time_millis), y=REAL_TIME_TO_SERVE_COMPOSITION))
ggplot(h1_ordered) + geom_line(aes(x=start_time_millis, y=REAL_TIME_TO_SERVE_COMPOSITION))
ggplot(h1_ordered) + geom_line(aes(x=start_time_millis, y=REAL_TIME_TO_SERVE_COMPOSITION)) + geom_smooth()
ggplot(h1_ordered, aes(x=start_time_millis, y=REAL_TIME_TO_SERVE_COMPOSITION)) + geom_line() + geom_smooth()
h1 <- experiment_results_raw[experiment_results_raw$X == "h1_1k_1",]
h1_ordered <- relativeTime(h1[order(h1[,c("start_time_millis")]),])
seconds_breaks <- seq(from=1000,to=max_time, by=10000)
previous_break <- 0
real_composition_times <- data.frame()
index <- 0
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (nrow(selected_measurements) > 0 ) {
real_tempdf <- cbind(id=index,  selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
index <- index + 1
}
ggplot(real_composition_times) + geom_boxplot(aes(x=factor(id), y=REAL_TIME_TO_SERVE_COMPOSITION)) + facet_grid(. ~ composition_size)
avg_real_time_comp <- lapply(real_composition_times$REAL_TIME_TO_SERVE_COMPOSITION, mean)
myts <- ts(avg_real_time_comp, start=1, end=2, frequency=1)
plot(myts)
avg_real_time_comp
myts <- ts(unlist(avg_real_time_comp), start=1, end=2, frequency=1)
plot(myts)
avg_real_time_comp <- lapply(real_composition_times[real_composition_times$composition_size==2,c("REAL_TIME_TO_SERVE_COMPOSITION")], mean)
myts <- ts(unlist(avg_real_time_comp), start=1, end=2, frequency=1)
plot(myts)
myts
unlist(avg_real_time_comp)
length(unlist(avg_real_time_comp))
myts <- ts(unlist(avg_real_time_comp), start=1, end=322, frequency=1)
plot(myts)
h1 <- experiment_results_raw[experiment_results_raw$X == "h1_1k_1",]
h1_ordered <- relativeTime(h1[order(h1[,c("start_time_millis")]),])
seconds_breaks <- seq(from=1000,to=max_time, by=10000)
previous_break <- 0
real_composition_times <- data.frame()
index <- 0
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (nrow(selected_measurements) > 0 ) {
real_tempdf <- cbind(id=index,  selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
index <- index + 1
}
ggplot(real_composition_times) + geom_boxplot(aes(x=factor(id), y=REAL_TIME_TO_SERVE_COMPOSITION)) + facet_grid(. ~ composition_size)
avg_real_time_comp <- lapply(real_composition_times[real_composition_times$composition_size==2,c("REAL_TIME_TO_SERVE_COMPOSITION")], mean)
myts <- ts(unlist(avg_real_time_comp), start=1, end=322, frequency=1)
str(myts)
fit <- stl(myts, s.window="period")
?stl
myts <- ts(unlist(avg_real_time_comp), start=1, end=161, frequency=2)
fit <- stl(myts, s.window="period")
plot(fit)
monthplot(myts)
seasonplot(myts)
library(forecast)
plot.ts(myts)
h1 <- experiment_results_raw[experiment_results_raw$X == "h1_16k_1",]
h1_ordered <- relativeTime(h1[order(h1[,c("start_time_millis")]),])
seconds_breaks <- seq(from=1000,to=max_time, by=10000)
previous_break <- 0
real_composition_times <- data.frame()
index <- 0
for (current_break in seconds_breaks) {
#real_composition_times[[length(real_composition_times) + 1]] <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION")]
selected_measurements <- h1_ordered[ h1_ordered$start_time_millis >= previous_break & h1_ordered$start_time_millis <= current_break, c("REAL_TIME_TO_SERVE_COMPOSITION", "composition_size")]
if (nrow(selected_measurements) > 0 ) {
real_tempdf <- cbind(id=index,  selected_measurements)
real_composition_times <- rbind(real_composition_times, real_tempdf)
previous_break <- current_break
}
index <- index + 1
}
View(executions_data_summary)
overtime_plot  <- ggplot(real_composition_times) +
geom_boxplot(aes(x=factor(id), y=REAL_TIME_TO_SERVE_COMPOSITION)) +
facet_grid(. ~ composition_size) +
labs(title="Evolution of averages, time interval 10s, 16k nodes", x="Experiment time", y="Average Composition Time (ms)")
ggsave(filename="plots/h1_16k_overtime.pdf",
plot=overtime_plot,
height=13,
units="cm")
